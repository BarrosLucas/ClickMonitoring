import 'package:flutter/material.dart';

class Acompanhar extends StatelessWidget {
  const Acompanhar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Acompanhar"),);
  }
}
